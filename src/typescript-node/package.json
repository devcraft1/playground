{
  "name": "test-mail",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "dev": "yarn run nodemon",
    "build": "rimraf ./build && tsc",
    "start": "yarn run build && node build/index.js"
  },
  "dependencies": {
  },
  "devDependencies": {
    "rimraf": "^4.0.4",
    "@types/node": "^18.11.18",
    "nodemon": "^2.0.20",
    "typescript": "^4.9.4"
  }
}
